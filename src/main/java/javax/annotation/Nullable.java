package javax.annotation;

/**
 *  note: because compiler use ajc not javac,
 *  so add this annotation to avoiding code review throw exception when code compiling.
 */
public @interface Nullable {
}
